{
  "name": "@blink402/helius",
  "version": "0.1.0",
  "description": "Helius API integration for wallet analysis",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "@blink402/solana": "workspace:*",
    "@blink402/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
